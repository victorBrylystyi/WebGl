// ---------------- _160221 ----------------
// Реализация на 2-х буферах, в которых сложены методом "concat" все вершины и все цвета.
// Опрашиваются буфера по рассчитанным смещениям.
// Без установки цвета не работает.
// Программа создается в WebGlRender.

// В следующей версии:
// - при отрисовке в буфер вершин и цвета будут записаны данные конкретных объектов (без смещения)
// - под разные комбинации создаются разные программы

// ---------------- _230221 ----------------
// под каждую программу (материал), создаются статические буферы для каждого объекта отдельно 
// созданы различные материалы 
// для окраски куба в цвета необходимо использовать BasicMaterial
// концептуально необходимо пересмотреть и переделать структуру и логику программы 

// В следующей версии:
// - необходимо переделать материалы и структуру в целом т.к. текущая структура непозволяет добавить свет корректно
// - геометрия в себе несет данные о координатах текстуры и/или(?) цвета вершин (!)для каждой грани 
// - базовый класс материалы вероятно должны обладать цветом и текстурой. 
// - BasicMaterial - материал с цветом и/или текстурой без света и теней 
// - PhongMaterial - материал с цветом и/или текстурой со светом и тенями
// - свет добавляется через scene.add(light)
// - хочу проверить, что достаточно 1 буфера нормалей для всех объектов 
// - возможно код шейдеров придется парсить и конкатенировать

// ---------------- _020321 ----------------
// преобразована концепция материалов и геометрии. есть Базовый материал (просто цвет или текстура) 
//     и Фонг материал (с учетом освещения(на модели Фонга))
// оптимизация отображения правил в текстурах 
// свет через scene.add(light)
// 3 источника освещения: фоновое, направленное, точечное (без укзакания цели освещения)

// В следующей версии:
// - добавление теней методом Shadow map
// - добавление datGUI 
// - добавить к освещению цель 

// ---------------- _150321 ----------------

// исправлены ошибки в программах вершинных шейдеров PhongMaterial в вычислении вершины в мировых координатах
// направленный свет имеет цель освещения. цель по своей сути представляет определение направления в зависимости от 
//      цели и текущих координат света
// использовал неудачно тень. необходимо переделать текстуры (добавить абстрактный класс) 
//   и использовать 1 шейдерный код в материалах

// ---------------- _180321 ----------------

// переделка идеологии ОПЯТЬ !!!! АААаААААААААААА. 1 шейдерный код на материал; возможность рекомпиляции 
// получать тип юниформ переменных с целью создать шаблон передаваемых данных 
// оставляю рабочую версию со старой идеологией (на этот момент - текущая)

// ---------------- _190321 ----------------





